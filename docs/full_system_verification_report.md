# Full-System Verification Audit Report

**Status:** ALL VIOLATIONS RESOLVED · 100% COMPLIANT

**Objective:** Audit all displayed numbers, metrics, and comparative claims across the InsightVision frontend to ensure they are dynamically sourced from actual pipeline runs or explicitly tagged as external literature.

---

## 1. Dashboard (`Dashboard.tsx`)
*   **GPU Utilization (`gpuUsage`):** Bound to `vram.pct` derived dynamically from the backend `/api/benchmark` diagnostics (`unified_pipeline_active_peak` / `hardware_limit`). **COMPLIANT**
*   **Recent Activity Log:** Dynamically renders the most recent live WebSocket message events (`messages`). Falls back to a clean `"System idle — ready for tracking session"` state when no active session has run. **COMPLIANT**
*   **Telemetry Cards:**
    *   *Tracking Rate:* Dynamically resolved via `getTrackingFps()` querying the active tracker's benchmark data (`botsort` vs `sam3_native`). **COMPLIANT**
    *   *Reasoning Core:* Dynamically displays the VLM model identifier and the active localizer's VLM OCR success rate. **COMPLIANT**
    *   *Hardware Engine VRAM:* Bound to dynamic `vram.used` and `vram.total` values. **COMPLIANT**
    *   *GPU Engine Model Name:* Dynamically queries the host GPU device name from PyTorch (`torch.cuda.get_device_name(0)`). Falls back to `"RTX 5070 Ti"` (the host GPU) if the API hasn't resolved. **COMPLIANT**

## 2. Live Camera (`LiveCamera.tsx`)
*   **Active Tracking Nodes Bar:** Metrics like confidence (`t.confidence`) and track IDs are fetched live via the session WebSocket channel. **COMPLIANT**

## 3. Session History (`History.tsx`)
*   **Fallback History Data:** Mock metrics have been entirely removed. If the backend fails or does not contain session logs, the page cleanly falls back to an empty list showing `"No sessions found"`. **COMPLIANT**
*   **Timeline Logs:** Timestamps and tracking/OCR/segmentation latencies are dynamically generated by the backend on active runs. **COMPLIANT**

## 4. Performance & Telemetry (`Performance.tsx`)
*   **Fallback Data:** Reset to `0.0` default entries. No fake claims are made if the backend is offline. **COMPLIANT**
*   **Accuracy & Telemetry Cards:**
    *   *mAP IoU, OCR Success Rate, and Track ID consistency:* All bound directly to the dynamic `data.localizer_comparison` and `data.tracker_comparison` payloads fetched from `/api/benchmark`. **COMPLIANT**

## 5. Comparative Analysis (`ComparativeAnalysis.tsx`)
*   **Fallback Data:** Reset to clean `0.0` default structures. **COMPLIANT**
*   **Hardware requirements & detailed capability descriptions:** Bound directly to `vramPeakGB` dynamically calculated from the benchmark payload rather than hardcoding static VRAM estimates. **COMPLIANT**
*   **Specification Matrix Table:** Bounded to dynamic localizer entries (`grounding_dino.ocr_success_rate` / `sam3.ocr_success_rate`) in the JSX. **COMPLIANT**

---

## Conclusion
Every single metric displayed in the InsightVision frontend is verified as dynamic. Any fallback or default state displays either actual system specifications or empty status states, guaranteeing that the defense panel sees only genuine, benchmarked data.
